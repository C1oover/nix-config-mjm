variables:
  GIT_CHECKOUT: "false"
  GIT_DEPTH: "0"
stages: [update]
update megamerge:
  tags: [nix]
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"
  stage: update
  resource_group: megamerge
  script: update-fork --upstream $UPSTREAM
